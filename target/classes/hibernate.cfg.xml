<?xml version="1.0" encoding="UTF-8"?>
<hibernate-configuration>
  <session-factory>

    <!-- Connexion H2 locale (fichier pour persistance) -->
    <property name="hibernate.connection.driver_class">org.h2.Driver</property>
    <property name="hibernate.connection.url">jdbc:h2:file:./data/annuaire-db;AUTO_SERVER=TRUE;DB_CLOSE_DELAY=-1</property>
    <property name="hibernate.connection.username">sa</property>
    <property name="hibernate.connection.password"></property>

    <!-- Dialecte -->
    <property name="hibernate.dialect">org.hibernate.dialect.H2Dialect</property>

    <!-- Schéma en dev -->
    <property name="hibernate.hbm2ddl.auto">update</property>

    <!-- Debug SQL -->
    <property name="hibernate.show_sql">true</property>
    <property name="hibernate.format_sql">true</property>

    <!-- Entités -->
    <mapping class="com.exemple.models.Services" />
    <mapping class="com.exemple.models.Admin" />
    <mapping class="com.exemple.models.Employee" />
    <mapping class="com.exemple.models.Sites" />

  </session-factory>
</hibernate-configuration>

